// React-Frontend, responsive/mobile-first, als PWA (siehe docs/architecture.md Abschnitt 2).
// Spricht ausschliesslich ueber REST/OpenAPI mit dem api-gateway - keine direkte
// Backend-Code-Kopplung.
import React from 'react';
import ReactDOM from 'react-dom/client';

function App() {
  return <div>WaelderBytes ERP V2 - Platzhalter</div>;
}

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
