# api-gateway

Einstiegspunkt fuer alle eingehenden Requests: Routing zu den Fach-Services (auth-service,
kuenftig erp-service etc.), zentraler Auth-Check (JWT-Validierung), Rate-Limiting.

Stand: Platzhalter, noch kein echtes Routing implementiert.
