{
  "name": "erp-v2",
  "version": "0.1.0",
  "private": true,
  "description": "WaelderBytes ERP V2 - Nx-Monorepo (NestJS Backend-Services, React-PWA Frontend)",
  "scripts": {
    "dev:auth-service": "npm run start:dev --workspace=apps/auth-service",
    "dev:api-gateway": "npm run start:dev --workspace=apps/api-gateway",
    "dev:web": "npm run dev --workspace=apps/web"
  },
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  }
}
